/**
 * 项目常量配置
 */

/**
 * 默认端口号
 * 可以通过环境变量 PORT 覆盖
 */
export const DEFAULT_PORT = 4567;

/**
 * 默认主机地址
 * 可以通过环境变量 HOST 覆盖
 */
export const DEFAULT_HOST = "0.0.0.0";

/**
 * 默认缓存目录
 * 可以通过环境变量 CACHE_DIR 覆盖
 */
export const DEFAULT_CACHE_DIR = "/cache";

/**
 * 默认缓存最大大小 (GB)
 * 可以通过环境变量 CACHE_MAX_SIZE 覆盖
 */
export const DEFAULT_CACHE_MAX_SIZE = 4;

/**
 * 默认NPM注册表
 * 可以通过环境变量 NPM_REGISTRY 覆盖
 */
export const DEFAULT_NPM_REGISTRY = "https://registry.npmjs.org/";

/**
 * 默认NPM最大TGZ包大小 (MB)
 * 可以通过环境变量 NPM_MAX_TGZ_SIZE 覆盖
 */
export const DEFAULT_NPM_MAX_TGZ_SIZE = 100;